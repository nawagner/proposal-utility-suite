# API key for OpenRouter requests. Required.
OPENROUTER_API_KEY=

# Optional: Default model for chat completions and other API calls
OPENROUTER_DEFAULT_MODEL=gpt-5

# Optional: Specific model for proposal reviews (defaults to openai/gpt-5)
OPENROUTER_REVIEW_MODEL=openai/gpt-5

# Optional: Custom OpenRouter API base URL (for proxies or alternative endpoints)
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

# Optional: Your application URL for OpenRouter's HTTP-Referer header
# Set this to your production URL when deploying (e.g., https://your-app.vercel.app)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Supabase Configuration (Required for rubric storage and authentication)
# Get these from your Supabase project settings: https://app.supabase.com/project/_/settings/api

# Supabase project URL
PROPOSAL_SUPABASE_URL=
NEXT_PUBLIC_PROPOSAL_SUPABASE_URL=

# Supabase anon/public key (for client-side auth)
PROPOSAL_SUPABASE_ANON_KEY=
NEXT_PUBLIC_PROPOSAL_SUPABASE_ANON_KEY=

# Supabase service role key (for server-side operations, keep secret!)
PROPOSAL_SUPABASE_SERVICE_ROLE_KEY=

# When deploying on Vercel, set these in the project environment variables UI.
